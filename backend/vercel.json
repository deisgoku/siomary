{
    "builds": [
        {
            "src": "/app/main.py",
            "use": "@vercel/python"
        }
    ],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "/app/main.py"
        },
        {
            "src": "/(.*)",
            "dest": "/app/bot.py"
        }
    ]
}
